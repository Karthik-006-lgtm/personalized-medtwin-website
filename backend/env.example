## Backend environment variables (example)
## Copy to `backend/.env` (do NOT commit `.env` — it is already in `.gitignore`)

# Server
PORT=5000

# Database
MONGODB_URI=mongodb://localhost:27017/health-monitor

# Auth
JWT_SECRET=change-me

# Prescription Analyzer (Gemini Vision - best for handwriting)
# Set your key locally in backend/.env (never paste keys into chat or commit them)
GEMINI_API_KEY=YOUR_GEMINI_KEY_HERE

# Optional (keep default unless you know you need another vision-capable model)
GEMINI_VISION_MODEL=models/gemini-2.5-flash

